exec-once = @uwsm@/bin/uwsm-app -- @hypridle@/bin/hypridle
# exec-once = uwsm-app -- mako     # same
exec-once = @uwsm@/bin/uwsm-app -- @waybar@/bin/waybar
# exec-once = uwsm-app -- fcitx5   # TODO: haven't done fci yet
exec-once = @uwsm@/bin/uwsm-app -- @swaybg@/bin/swaybg -i ~/.config/omarchy/current/background -m fill
# exec-once = uwsm-app -- swayosd-server  # service
# exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1  # TODO: i _think_ this is enabled by nix except now i've tried it and i don't think it's running so go double check options
# exec-once = omarchy-cmd-first-run  # TODO: port this script

# Slow app launch fix -- set systemd vars
exec-once = systemctl --user import-environment $(env | cut -d'=' -f 1)
exec-once = dbus-update-activation-environment --systemd --all
