# GitHub Repository

This is a fork of the upstream `basecamp/omarchy` repository.

- **Origin**: `codingismy11to7/omarchy` - PRs should be opened here
- **Upstream**: `basecamp/omarchy` - the original project
- **Default branch**: `nix` - PRs should target this branch, not `master`

When creating pull requests:
- Use `codingismy11to7` as the owner, not `dhh` or `basecamp`
- Use `nix` as the base branch, not `master`
