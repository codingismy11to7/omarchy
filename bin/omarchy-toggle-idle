#!@bash@/bin/bash

if pgrep -x hypridle >/dev/null; then
  pkill -x hypridle
  @libnotify@/bin/notify-send "Stop locking computer when idle"
else
  @uwsm@/bin/uwsm-app -- @hypridle@/bin/hypridle >/dev/null 2>&1 &
  @libnotify@/bin/notify-send "Now locking computer when idle"
fi
