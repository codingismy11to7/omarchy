#!@bash@/bin/bash

fzf_args=(
  --multi
  --preview 'nix eval --offline nixpkgs#{1}.meta --json | @jq@/bin/jq'
  --preview-label='alt-p: toggle description, alt-j/k: scroll, tab: multi-select'
  --preview-label-pos='bottom'
  --preview-window 'down:65%:wrap'
  --bind 'alt-p:toggle-preview'
  --bind 'alt-d:preview-half-page-down,alt-u:preview-half-page-up'
  --bind 'alt-k:preview-up,alt-j:preview-down'
  --color 'pointer:green,marker:green'
)

pkg_names=$(stdbuf -o0 nix search --offline nixpkgs '^' 2>/dev/null |
  @gnused@/bin/sed -u 's/\x1b\[[0-9;]*m//g' |
  @gawk@/bin/awk '/^\* /{print $2; fflush()}' |
  @gnused@/bin/sed -u 's/^legacyPackages\.[^.]*\.//' |
  @fzf@/bin/fzf "${fzf_args[@]}")

if [[ -n "$pkg_names" ]]; then
  # Prefix each package with nixpkgs# and run nix profile add
  export NIXPKGS_ALLOW_UNFREE=1
  echo "$pkg_names" | @gnused@/bin/sed 's/^/nixpkgs#/' | xargs nix profile add --impure
  omarchy-show-done
fi
