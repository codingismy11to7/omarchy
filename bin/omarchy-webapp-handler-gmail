url="$1"
web_url="https://mail.google.com"

# Handle mailto: URLs
if [[ $url =~ ^mailto: ]]; then
  email=$(echo "$url" | @sed@ 's/mailto://')
  web_url="https://mail.google.com/mail/?view=cm&to=$email"
fi

exec omarchy-launch-webapp "$web_url"
