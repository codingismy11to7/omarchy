
if [[ -f ~/.config/alacritty/alacritty.toml ]]; then
  touch ~/.config/alacritty/alacritty.toml
fi

if @pgrep@ -f '(^|/)kitty( |$)' >/dev/null; then
  @pkill@ -SIGUSR1 -f '(^|/)kitty( |$)'
fi

if @pgrep@ -f '(^|/)ghostty( |$)' >/dev/null; then
  @pkill@ -SIGUSR2 -f '(^|/)ghostty( |$)'
fi
